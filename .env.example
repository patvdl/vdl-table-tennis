# Copy to .env and fill in from your Supabase project (Settings -> API).
# If these are missing the app runs in local demo mode (data stored in your browser).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
